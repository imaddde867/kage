# LLM — Ollama backend (HTTP, Metal GPU via llama.cpp)
ollama
requests

# LLM — MLX backend (Apple Neural Engine + GPU, direct Python, no daemon)
# Install: pip install mlx-lm
# Models: https://huggingface.co/mlx-community
mlx-lm

# TTS — Kokoro-82M via MLX Audio
mlx-audio[tts]

# Speech to text — Apple native (fast, hardware-accelerated, no model load)
SpeechRecognition
# Speech to text fallback — local Whisper
faster-whisper

# Audio recording
sounddevice
numpy

# Wake word detection
openwakeword

# Utilities
python-dotenv
