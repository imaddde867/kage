# LLM
ollama
requests

# Speech to text — Apple native (fast, hardware-accelerated, no model load)
SpeechRecognition
# Speech to text fallback — local Whisper
faster-whisper

# Audio recording
sounddevice
numpy

# Wake word detection
openwakeword

# Utilities
python-dotenv
